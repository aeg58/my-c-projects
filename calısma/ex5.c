#include <stdio.h>

int main(void)
{
    int sayilar[5];
    int i, toplam = 0;
    float ort;

    printf("5 sayi girin : \n");
    for (i = 0; i < count; i++)
    {
        scanf("%d",&sayilar[i]);
    }

    ort = (float)toplam/5;

    printf("toplam: %d\n",toplam);
}
