#include <stdio.h>

int main(void)
{
    int sayilar[5];
    int i, temp;
    for(i = 0; i < 5; i++)
    {
        printf("%d. sayiyi giriniz: ", i + 1);
        scanf("%d", &sayilar[i]);
    }

    for ( i = 0; i < 5-i; i++)
    for (size_t i = 0; i < count; i++)
    
    

    
}
