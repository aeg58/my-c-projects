#include <stdio.h>

struct Student {
    char name[50];
    int age;
    float grade;
};

int main() {
    struct Student s1;

    // buraya 3 satır: s1.name, s1.age, s1.grade değerlerini ata

    // buraya 1 satır: printf ile ekrana bastır
}
